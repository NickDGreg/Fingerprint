# Core Beliefs

- Explicit beats clever.
- Observable failures beat hidden retries.
- Small semantically named modules are faster for humans and agents to navigate.
- Bounded network behavior is mandatory when interacting with hostile sites.
- Compatibility wrappers are acceptable when they reduce migration risk, but the canonical implementation must live in the package layout.
